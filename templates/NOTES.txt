1. Build and push images/postfix, images/dovecot, images/rspamd and images/ldap.
2. Ensure A and PTR records point {{ .Values.mail.hostname }} to the LoadBalancer IP.
3. Ensure ports 80, 25, 465, 587 and 993 are reachable.
4. Watch initial certificate issuance:
   kubectl -n {{ .Release.Namespace }} logs deploy/{{ include "mailserver.fullname" . }}-postfix -c obtain-certificate -f
5. Add the first LDAP user:
   NAMESPACE={{ .Release.Namespace }} RELEASE={{ include "mailserver.fullname" . }} ./scripts/ldap-user.sh
