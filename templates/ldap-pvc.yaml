apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mailserver.fullname" . }}-ldap
  labels:
    {{- include "mailserver.labels" . | nindent 4 }}
spec:
  accessModes: [ReadWriteOnce]
  {{- with .Values.persistence.ldap.storageClass }}
  storageClassName: {{ . | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.ldap.size }}
