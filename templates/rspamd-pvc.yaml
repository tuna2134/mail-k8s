apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mailserver.fullname" . }}-rspamd
  labels:
    {{- include "mailserver.labels" . | nindent 4 }}
spec:
  accessModes: [ReadWriteOnce]
  {{- with .Values.persistence.rspamd.storageClass }}
  storageClassName: {{ . | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.rspamd.size }}
