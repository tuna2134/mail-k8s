{{- if .Values.sogo.enabled }}
{{- $n := include "mailserver.fullname" . -}}
apiVersion: v1
kind: Service
metadata: { name: {{ $n }}-sogo }
spec:
  selector: { app.kubernetes.io/name: {{ $n }}, app.kubernetes.io/component: sogo }
  ports: [{ name: https, port: 8443, targetPort: https }]
{{- end }}
